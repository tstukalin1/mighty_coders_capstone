# SQL Features That Save Code

**Capstone Project 1 (Open Source)** — Constructor University, in partnership with the MariaDB Foundation.

## Project Description

This project demonstrates two of MariaDB's built-in SQL features that remove work you would otherwise write in application code:

1. **`CREATE OR REPLACE`** — idempotent DDL. A table (or database) can be redefined any number of times without a manual `DROP TABLE IF EXISTS` step first. This directly solves an error our team faces while testing: re-running a plain `CREATE DATABASE` script failed with `ERROR 1007: database exists`, because the script wasn't idempotent.
2. **`RETURNING`** — `INSERT` and `DELETE` statements hand back the rows they touched, removing the need for a second round-trip `SELECT` to see what just changed.

Each feature is shown with a **before/after comparison**: what the same task looks like without the feature, and what it looks like with it.

**A finding:** we initially assumed `RETURNING` also worked on `UPDATE`, since it's a natural extension of the same idea. Testing it directly on MariaDB 12.3.3 produced a syntax error (`ERROR 1064`). MariaDB's own documentation confirms `RETURNING` is only supported on `INSERT`, `REPLACE`, and `DELETE` — not `UPDATE`. We kept this in the script as a documented negative result, since it's a useful thing to know before relying on this feature.

Both are standard MariaDB features not available in MySQL, or only available there with extra boilerplate.

## Team Members

**Mighty Coders**

* Timofey Stukalin
* Panackal Devassia, Dinu
* Yosief Gebrewahid Kidane

## MariaDB Version

```text
MariaDB 12.3.3-MariaDB (Homebrew)
```

## Setup Instructions

1. Install MariaDB (macOS):
   ```bash
   brew install mariadb
   brew services start mariadb
   ```

2. Clone this repository:
   ```bash
   git clone https://github.com/playboifufalo/mighty_coders_capstone.git
   cd mighty_coders_capstone
   ```

3. Run the script:
   ```bash
   mariadb -u root -p < sql_features_that_save_work.sql
   ```
   This creates the `mighty_coders` database and `team_members` table, and runs all the example queries automatically — no separate dataset file needed. You can re-run this command as many times as you like without errors, thanks to `CREATE OR REPLACE`.

## Dataset

Sample data generated by the team (team member names, matriculation numbers, ages), included directly in `sql_features_that_save_work.sql`. No external dataset is required.

## Repository Structure

```text
.
├── README.md
├── sql_features_that_save_work.sql   # Area 1 demo: CREATE OR REPLACE + RETURNING
├── sql_yosief.sql                     # Week 1 exploratory example (system-versioned tables)
└── LICENSE
```

## Testing / Validation

### Before/After Comparison

**`CREATE OR REPLACE`:**
- *Without it:* every re-run of a setup script needs `DROP TABLE IF EXISTS` (or worse, a manual `DROP DATABASE`) before the `CREATE`, or it fails outright — exactly the error we hit during testing this week.
- *With it:* the same script can be re-run any number of times with a single `CREATE OR REPLACE TABLE` statement, no error, no manual cleanup step.

**`RETURNING`:**
- *Without it:* inserting a row and then confirming what was inserted takes two statements — an `INSERT` followed by a `SELECT ... WHERE` to find the row again, guessing at which row was just created.
- *With it:* a single `INSERT ... RETURNING` statement returns the new row immediately, no second query needed. Same applies to `DELETE ... RETURNING` — confirming what was removed in one step.
- *Finding:* `UPDATE ... RETURNING` is **not** supported in MariaDB (confirmed by testing, and by MariaDB's own docs) — only `INSERT`, `REPLACE`, and `DELETE` support it.

### How to Reproduce Our Tests

```bash
mariadb -u root -p < sql_features_that_save_work.sql
```

Run it once, then run it again immediately — with `CREATE OR REPLACE`, the second run succeeds with no errors, unlike a plain `CREATE TABLE` / `CREATE DATABASE` script.

### Known Limitations

* The sample dataset is small and generated for demonstration purposes, not production-scale.
* `RETURNING` is demonstrated on a small number of rows; its main practical benefit (avoiding round trips) matters more at scale or under network latency, which this local demo doesn't measure directly.

